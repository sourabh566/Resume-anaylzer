{
  "name": "resume-analyzer",
  "version": "1.0.0",
  "description": "AI-powered Resume Analyzer - Full-stack web application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"npm run server:dev\" \"npm run frontend:dev\"",
    "server": "node server.js",
    "server:dev": "nodemon server.js",
    "frontend:dev": "cd frontend && npm start",
    "frontend:build": "cd frontend && npm run build",
    "build": "npm run frontend:build && cd backend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["resume", "analyzer", "ai", "nlp", "react", "nodejs"],
  "author": "Resume Analyzer Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  }
}
